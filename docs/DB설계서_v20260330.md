# DB 설계서 (v20260330 / 현행화 완료)

본 문서는 IT 용어 사전 및 그룹웨어 시스템의 데이터 구조를 정의하며, 시스템 전반에 적용되는 공통 규격과 테이블별 상세 설계를 포함합니다.
(기존 `docs/DB설계서_최종본.md` 대비 2026-03-30까지의 모든 스키마 변경 사항을 반영한 버전입니다.)

---

## 0. 공통 시스템 규격 (System Global)

모든 테이블은 별도의 명시가 없더라도 데이터의 투명한 관리와 추적을 위해 다음 **공통 시스템 컬럼**을 포함합니다.

### [공통 시스템 컬럼]

- `id`: UUID (Primary Key, 자동 생성)
- `created_at`: TIMESTAMP (생성 시간, Default now())
- `updated_at`: TIMESTAMP (수정 시간, Default now(), 트리거 자동 갱신)
- `deleted_at`: TIMESTAMP (소프트 삭제 시간, NULL이면 활성 데이터)

### [공통 ENUM 타입]

- **`USER_ROLE`**: `SUPER_ADMIN` (최상위 관리자), `ORG_ADMIN` (조직 관리자), `TEAM_ADMIN` (팀 관리자), `USER` (일반 사용자)
- **`USER_STATUS`**: `ACTIVE` (활성), `OFFLINE` (오프라인), `VACATION` (휴가/반차), `MEETING` (회의 중)
- **`DOC_STATUS`**: `DRAFT` (임시저장), `PENDING` (결재대기), `APPROVED` (승인완료), `REJECTED` (반려)
- **`APPROVAL_STEP_STATUS`**: `WAITING` (대기), `PENDING` (결재 중), `APPROVED` (승인), `REJECTED` (반려)
- **`SOURCE_TYPE`**: `DOCUMENTS` (결재문서), `MEETING_LOGS` (회의록)

---

## 1. 기반 시스템 (Foundation)

### **Table:** `teams` (부서 및 팀 정보) [NEW]

> 시스템 공통 컬럼 포함

| **컬럼명** | **타입** | **설명** | **비고** |
| --- | --- | --- | --- |
| `name` | VARCHAR | 팀/부서 명칭 | 필수 |
| `description` | TEXT | 팀 설명 | |

### **Table:** `users` (사용자 및 조직도)

> 시스템 공통 컬럼 포함

| **컬럼명** | **타입** | **설명** | **비고** |
| --- | --- | --- | --- |
| `name` | VARCHAR | 사용자 이름 | 필수 |
| `department` | VARCHAR | 소속 부서 (레거시 필드) | `team_id` 연동 권장 |
| `role` | USER_ROLE | 사용자 권한 그룹 | RBAC 기반 제어 |
| `team_id` | UUID | 소속 팀 ID | `teams.id` FK |
| `status` | USER_STATUS | 현재 상태 | 업무현황판 연동 |

---

## 2. 업무 코어 시스템 (Pod A & B)

### **Table:** `documents` (결재 문서)

> 시스템 공통 컬럼 포함

| **컬럼명** | **타입** | **설명** | **비고** |
| --- | --- | --- | --- |
| `author_id` | UUID | 문서 작성자 | `users.id` FK |
| `title` | VARCHAR | 기안서 제목 | |
| `content` | TEXT | 마크다운 본문 | |
| `status` | DOC_STATUS | 전체 결재 진행 상태 | |
| `submitted_at` | TIMESTAMP | 기안 상신 시간 | |
| `final_approved_at` | TIMESTAMP | 최종 승인 완료 시간 | |

### **Table:** `document_approval_steps` (결재 라인) [NEW]

> 시스템 공통 컬럼 포함

| **컬럼명** | **타입** | **설명** | **비고** |
| --- | --- | --- | --- |
| `document_id` | UUID | 관련 문서 ID | `documents.id` FK |
| `step_order` | INTEGER | 결재 순번 | 0부터 시작 |
| `step_label` | VARCHAR | 단계 명칭 | 검토, 승인 등 |
| `approver_id` | UUID | 결재 대상자 | `users.id` FK |
| `status` | APPROVAL_STEP_STATUS | 해당 단계 상태 | |
| `acted_at` | TIMESTAMP | 실제 결재 수행 시간 | |
| `comment` | TEXT | 결재 반려/승인 의견 | |

### **Table:** `document_cc_recipients` (참조자 관리) [NEW]

> 시스템 공통 컬럼 포함

| **컬럼명** | **타입** | **설명** | **비고** |
| --- | --- | --- | --- |
| `document_id` | UUID | 관련 문서 ID | `documents.id` FK |
| `recipient_id` | UUID | 참조 대상자 | `users.id` FK |

### **Table:** `schedules` (업무 일정)

> 시스템 공통 컬럼 포함

| **컬럼명** | **타입** | **설명** | **비고** |
| --- | --- | --- | --- |
| `user_id` | UUID | 일정 소유자 | `users.id` FK |
| `title` | VARCHAR | 일정 제목 | |
| `start_time` | TIMESTAMP | 시작 시간 | |
| `end_time` | TIMESTAMP | 종료 시간 | |

---

## 3. AI 및 지식망 시스템 (Pod C & D)

### **Table:** `meeting_logs` (회의록)

> 시스템 공통 컬럼 포함

| **컬럼명** | **타입** | **설명** | **비고** |
| --- | --- | --- | --- |
| `owner_id` | UUID | 회의 주최자 | `users.id` FK |
| `audio_url` | VARCHAR | 음성 파일 경로 | Supabase Storage |
| `stt_text` | TEXT | 원본 음성 전사본 | 기본 텍스트 |
| `is_refined` | BOOLEAN | AI 정제 완료 여부 | |
| `title` | VARCHAR | AI 추천 제목 | |
| `summary` | TEXT | AI 자동 요약본 | |
| `participants` | JSONB/ARRAY | 참석자 리스트 | |
| `action_items` | JSONB | 할 일 목록 | `jira_key`, `jira_url` 포함 가능 |

### **Table:** `knowledge_vectors` (지식 지도)

> **시스템 공통 컬럼 제외 사유:** 저장 공간 효율화를 위해 Hard Delete 정책 사용.

| **컬럼명** | **타입** | **설명** | **비고** |
| --- | --- | --- | --- |
| `source_type` | SOURCE_TYPE | 데이터 출처 | ENUM 사용 |
| `source_id` | UUID | 원본 데이터 ID | 다각형 참조 |
| `embedding` | VECTOR(1536) | 벡터 데이터 | OpenAI 임베딩 |
| `metadata` | JSONB | 검색 필터 데이터 | 부서, 제목, 내용 등 |

### **Table:** `workspace_extensions` (익스텐션 설정)

> 시스템 공통 컬럼 포함

| **컬럼명** | **타입** | **설명** | **비고** |
| --- | --- | --- | --- |
| `ext_name` | VARCHAR | 서비스 이름 | slack, jira 등 (Unique) |
| `is_active` | BOOLEAN | 활성화 여부 | |
| `config` | JSONB | 설정 정보 | API Key, Domain 등 |

---

## 설계 가이드라인 (업데이트)

1. **데이터 무결성:** 모든 상태값은 정의된 ENUM 내에서만 관리하며, 결재 로직 등은 `document_approval_steps`의 순위(`step_order`)를 엄격히 준수합니다.
2. **AI 데이터 영속성:** `meeting_logs`의 `action_items` 내에 생성된 Jira 티켓 정보(`jira_key`)가 존재할 경우, 해당 액션 아이템은 실시간 동기화 대상임을 명시합니다.
3. **조직 권한 관리:** `users.role`에 따라 팀 관리자(`TEAM_ADMIN`)는 소속 팀원의 일정 및 결재 현황에 대한 가시성을 확보합니다.
4. **벡터 동기화:** 원본 데이터(`documents`, `meeting_logs`)가 업데이트되거나 정제(`is_refined`)될 때마다 `knowledge_vectors`는 즉시 또는 배치 방식으로 재인덱싱되어야 합니다.
